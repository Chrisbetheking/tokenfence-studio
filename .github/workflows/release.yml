name: Legacy release workflow (manual notice only)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The legacy multi-platform release workflow is retired."
          echo "Use Actions → Chris Studio macOS Builds and Release for macOS packages."
