# Chris Studio v2.2.0 最终验收

## 已通过的核心链路

- 会话创建、切换、重命名和重启恢复
- DeepSeek 真流式输出和用户停止
- 中英文切换和 Codex 式侧栏
- Computer Use：TextEdit 创建无标题文档、输入批准文字、完成和紧急停止
- Apple Silicon 与 Intel Release 构建

## 最终回归

1. 短对话与长流式回答。
2. 会话重命名并重启。
3. Computer Use 完成 TextEdit 安全目标。
4. 可靠执行中心只显示一张父任务卡，子动作折叠在内部。
5. 失败任务可标记已处理并归档，旧失败不再永久占用角标。
6. 项目目录读取、Diff、备份写入和 Git 状态。
7. Skills 与 MCP `tools/call` 仍需明确批准。
8. Apple Silicon 为 `arm64`，Intel 为 `x86_64`。
